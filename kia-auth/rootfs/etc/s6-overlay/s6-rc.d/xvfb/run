#!/command/with-contenv bashio
# Xvfb — virtual framebuffer. Chromium renders into this, x11vnc scrapes it.
# 400x800 matches the mobile viewport Kia's login page expects.

bashio::log.info "Starting Xvfb on display :99 (400x800x24)"
exec /usr/bin/Xvfb :99 -screen 0 400x800x24 -nolisten tcp
